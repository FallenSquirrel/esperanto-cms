{% extends 'esperantoAdminBundle::dialog.html.twig' %}

{% block content %}
    <form method="POST" action="{{ path(admin.editRoute, { id: data.id }) }}" data-delete-url="{{ path(admin.deleteRoute, { id: data.id }) }}" data-preview-url="{{ path(admin.config.getParameter('preview_route'),{ id: 'preview' }) }}">
        {% include admin.config.getTemplate('form') %}
        {{ block('buttonRow') }}
    </form>
{% endblock %}

{% block buttons %}
    <button class="btn offset2 span2 close"><i class="icon-remove"></i> {{ 'label.cancel'|trans }}</button>
    <button class="btn span2 save" type="button"><i class="icon-ok"></i> {{ 'label.save'|trans }}</button>
    <button class="btn span2 delete" type="button"><i class="icon-trash"></i> {{ 'label.delete'|trans }}</button>
    <button class="btn span2 preview" type="button"><i class="icon-eye-open"></i> {{ 'label.preview'|trans }}</button>
{% endblock %}